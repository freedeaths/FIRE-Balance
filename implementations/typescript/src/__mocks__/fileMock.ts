/**
 * Mock for static file imports in tests
 *
 * When Jest encounters imports for images, fonts, or other static assets,
 * it will use this mock instead of trying to process the actual files.
 */
export default 'test-file-stub';
